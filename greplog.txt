src/components/SubcontractorsDashboard.tsx:          onClick={() => { if (userRole !== 'viewer') { resetForm(); setShowModal(true); } else { alert('غير مسموح لك بإضافة مقاول'); } }}
src/components/SubcontractorsDashboard.tsx:                        onClick={() => setExpandedId(expandedId === c.id ? null : c.id)}
src/components/SubcontractorsDashboard.tsx:                              <div className="relative group/info inline-block" onClick={(e) => e.stopPropagation()}>
src/components/SubcontractorsDashboard.tsx:                              onClick={(e) => { e.stopPropagation(); if (userRole !== 'viewer') startEdit(c); else alert('غير مسموح لك بالتعديل'); }}
src/components/SubcontractorsDashboard.tsx:                              onClick={(e) => { e.stopPropagation(); if (userRole !== 'viewer') handleDelete(c.id); else alert('غير مسموح لك بالحذف'); }}
src/components/SubcontractorsDashboard.tsx:                  onClick={() => setShowModal(false)}
src/components/SubcontractorsDashboard.tsx:                        onClick={addWorkItem}
src/components/SubcontractorsDashboard.tsx:                                  onClick={() => removeWorkItem(item.id)}
src/components/SubcontractorsDashboard.tsx:                                    onClick={() => addDiscount(item.id)}
src/components/SubcontractorsDashboard.tsx:                                          onClick={() => removeDiscount(item.id, disc.id)}
src/components/SubcontractorsDashboard.tsx:                  onClick={() => setShowModal(false)}
src/components/Sidebar.tsx:          onClick={() => setIsOpen(!isOpen)}
src/components/Sidebar.tsx:          onClick={() => setIsOpen(false)}
src/components/Sidebar.tsx:            onClick={() => {
src/components/Sidebar.tsx:                  onClick={() => {
src/components/Sidebar.tsx:                onClick={onChangeSite}
src/components/Sidebar.tsx:                onClick={onLogout}
src/components/SrsDocument.tsx:            onClick={() => setSrsSection('architecture')}
src/components/SrsDocument.tsx:            onClick={() => setSrsSection('erd')}
src/components/SrsDocument.tsx:            onClick={() => setSrsSection('rbac')}
src/components/SrsDocument.tsx:            onClick={() => setSrsSection('apis')}
src/components/SrsDocument.tsx:            onClick={() => setSrsSection('roadmap')}
src/components/SrsDocument.tsx:                    onClick={() => setSelectedTable(t.name)}
src/components/ExtractsTab.tsx:                 onClick={() => setSelectedProjectId(p.id)}
src/components/ExtractsTab.tsx:                onClick={() => { setSelectedProjectId(''); setSelectedExtract(null); }}
src/components/ExtractsTab.tsx:                  onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية إصدار مستخلص جديد') : () => setIsCreatingNewExtract(!isCreatingNewExtract)} 
src/components/ExtractsTab.tsx:                  onClick={handlePrint}
src/components/ExtractsTab.tsx:                          onClick={() => setNewExtForm({...newExtForm, showZeroItems: !newExtForm.showZeroItems})}
src/components/ExtractsTab.tsx:                         onClick={() => setIsCreatingNewExtract(false)}
src/components/ExtractsTab.tsx:                        onClick={handleAddNewExtract}
src/components/ExtractsTab.tsx:                  onClick={() => setSelectedExtract(ext)}
src/components/ExtractsTab.tsx:                        onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية حذف المستخلصات') : (e) => { e.stopPropagation(); handleDeleteExtract(ext.id); }} 
src/components/ExtractsTab.tsx:                    onClick={() => handleUpdateExtractParams({ showZeroItems: !selectedExtract.showZeroItems })}
src/components/ExtractsTab.tsx:                    onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => setIsEditingHeader(!isEditingHeader)} 
src/components/ExtractsTab.tsx:                    onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => setIsEditingSignatoryNames(!isEditingSignatoryNames)} 
src/components/ExtractsTab.tsx:                                onClick={() => saveSettings({ headerTexts: { ...settings.headerTexts, ownerLogo: '' } })}
src/components/ExtractsTab.tsx:                                onClick={() => saveSettings({ headerTexts: { ...settings.headerTexts, contractorLogo: '' } })}
src/components/ProjectsTab.tsx:              onClick={() => { setShowForm(true); setEditingId(null); }}
src/components/ProjectsTab.tsx:                  onClick={currentUserRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية حفظ البيانات') : handleSave} 
src/components/ProjectsTab.tsx:                        onClick={() => handleOpenUserAssignment(project)}
src/components/ProjectsTab.tsx:                        onClick={() => { setEditingId(project.id); setFormData(project); setShowForm(true); }}
src/components/ProjectsTab.tsx:                        onClick={() => handleDelete(project.id)}
src/components/ProjectsTab.tsx:                          onClick={() => handleToggleUserAccess(u.username)}
src/components/ProjectsTab.tsx:                onClick={currentUserRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية حفظ التعيينات') : handleSaveUserAssignment} 
src/components/ProjectsTab.tsx:              onClick={async () => {
src/components/CustodyManager.tsx:                  onClick={() => setStatusFilter('all')}
src/components/CustodyManager.tsx:                  onClick={() => setStatusFilter('active')}
src/components/CustodyManager.tsx:                  onClick={() => setStatusFilter('settled')}
src/components/CustodyManager.tsx:                      onClick={() => setSelectedEngineerId(cust.id)}
src/components/CustodyManager.tsx:                            onClick={(e) => {
src/components/CustodyManager.tsx:                          onClick={(e) => {
src/components/CustodyManager.tsx:                        onClick={() => setSettlingId(null)}
src/components/CustodyManager.tsx:                onClick={() => setConfirmState(null)} 
src/components/CustodyManager.tsx:                onClick={() => {
src/components/CustodyManager.tsx:                onClick={() => setConfirmState(null)}
src/components/AddTransactionModal.tsx:              onClick={onClose} 
src/components/AddTransactionModal.tsx:                          onClick={() => setIsNewRecipient(true)}
src/components/AddTransactionModal.tsx:                          onClick={() => {
src/components/AddTransactionModal.tsx:              onClick={onClose}
src/components/SuppliesRecordsTab.tsx:            onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية تسجيل توريدات جديدة') : handleOpenAdd}
src/components/SuppliesRecordsTab.tsx:                <th className="py-5 px-6 font-black text-slate-800 text-center cursor-pointer hover:bg-slate-200" onClick={() => requestSort('ticketNo')}>
src/components/SuppliesRecordsTab.tsx:                <th className="py-5 px-6 font-black text-slate-800 text-center cursor-pointer hover:bg-slate-200" onClick={() => requestSort('date')}>
src/components/SuppliesRecordsTab.tsx:                          onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية تعديل السجلات') : () => handleOpenEdit(rec)} 
src/components/SuppliesRecordsTab.tsx:                          onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية حذف السجلات') : () => handleDelete(rec.id)} 
src/components/SuppliesRecordsTab.tsx:                                  onClick={() => {
src/components/ErpSubDashboards.tsx:                onClick={() => setActivePersona(p.id)}
src/components/ContractorCertificates.tsx:          onClick={() => setShowAddForm(!showAddForm)}
src/components/ContractorCertificates.tsx:                      onClick={() => {
src/components/ContractorCertificates.tsx:                      onClick={() => {
src/components/ContractorCertificates.tsx:                    onClick={() => {
src/components/EquipmentSummaryTable.tsx:            onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الإضافة') : () => setShowAddEqModal(true)}
src/components/EquipmentSummaryTable.tsx:                  onClick={() => setSelectedEqId(eq.id)}
src/components/EquipmentSummaryTable.tsx:                  <td className="py-4 px-5 text-left" onClick={(e) => e.stopPropagation()}>
src/components/EquipmentSummaryTable.tsx:                         onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => handleStartEditEq(eq)}
src/components/EquipmentSummaryTable.tsx:                         onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDeleteEq(eq.id, eq.name)}
src/components/EquipmentSummaryTable.tsx:                  onClick={() => setSelectedEqId(eq.id)}
src/components/EquipmentSummaryTable.tsx:                  <td className="py-4 px-5 text-left" onClick={(e) => e.stopPropagation()}>
src/components/EquipmentSummaryTable.tsx:                         onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => handleStartEditEq(eq)}
src/components/EquipmentSummaryTable.tsx:                         onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDeleteEq(eq.id, eq.name)}
src/components/UsersAdminPanel.tsx:          onClick={() => setShowAddModal(true)} 
src/components/UsersAdminPanel.tsx:                          onClick={() => setUserToDelete(item)} 
src/components/UsersAdminPanel.tsx:                      onClick={() => handleOpenLogs(item)}
src/components/UsersAdminPanel.tsx:                        onClick={() => setEditingUser(item)}
src/components/UsersAdminPanel.tsx:            <div className="absolute inset-0" onClick={() => setSelectedUserForLogs(null)}></div>
src/components/UsersAdminPanel.tsx:                  onClick={() => setSelectedUserForLogs(null)}
src/components/UsersAdminPanel.tsx:                  onClick={() => setSelectedUserForLogs(null)}
src/components/UsersAdminPanel.tsx:                  onClick={() => setShowAddModal(false)}
src/components/UsersAdminPanel.tsx:                                    onClick={() => {
src/components/UsersAdminPanel.tsx:                      onClick={() => setIsAddingProject(!isAddingProject)}
src/components/UsersAdminPanel.tsx:                          onClick={handleAddProject}
src/components/UsersAdminPanel.tsx:                          onClick={() => setIsAddingProject(true)}
src/components/UsersAdminPanel.tsx:                              onClick={() => {
src/components/UsersAdminPanel.tsx:                              onClick={() => {
src/components/UsersAdminPanel.tsx:                  onClick={() => handleUpdateUser(editingUser.username, editingUser)}
src/components/UsersAdminPanel.tsx:                  onClick={() => setEditingUser(null)} 
src/components/UsersAdminPanel.tsx:                    onClick={generateCaptcha}
src/components/UsersAdminPanel.tsx:                  onClick={handleDeleteSite} 
src/components/UsersAdminPanel.tsx:                  onClick={() => setSiteToDelete(null)} 
src/components/SuppliesAccountTab.tsx:                  onClick={() => setExpandedSupplierId(isExpanded ? null : s.ids[0])}
src/components/SiteSelectionScreen.tsx:                onClick={onToggleOfflineBypass}
src/components/SiteSelectionScreen.tsx:            onClick={onLogout}
src/components/SiteSelectionScreen.tsx:              onClick={() => setViewMode('hierarchy')}
src/components/SiteSelectionScreen.tsx:              onClick={() => setViewMode('grid')}
src/components/SiteSelectionScreen.tsx:                  onClick={() => {
src/components/SiteSelectionScreen.tsx:                        onClick={() => {
src/components/SiteSelectionScreen.tsx:                      onClick={() => {
src/components/SiteSelectionScreen.tsx:                      onClick={() => setAdminSearchQuery('')} 
src/components/SiteSelectionScreen.tsx:                            onClick={() => {
src/components/SiteSelectionScreen.tsx:                              onClick={() => {
src/components/SiteSelectionScreen.tsx:                                        onClick={(e) => {
src/components/SiteSelectionScreen.tsx:                                        onClick={(e) => {
src/components/SiteSelectionScreen.tsx:                      onClick={() => setSiteSearchQuery('')} 
src/components/SiteSelectionScreen.tsx:                              onClick={() => setSelectedSiteIdForHierarchy(site.id)}
src/components/SiteSelectionScreen.tsx:                                onClick={() => {
src/components/SiteSelectionScreen.tsx:                            onClick={() => {
src/components/SiteSelectionScreen.tsx:                              onClick={() => setProjectSearchQuery('')} 
src/components/SiteSelectionScreen.tsx:                onClick={() => onSiteSelected(site)}
src/components/SiteSelectionScreen.tsx:                    <div className="flex gap-1.5 no-print" onClick={(e) => e.stopPropagation()}>
src/components/SiteSelectionScreen.tsx:                        onClick={(e) => {
src/components/SiteSelectionScreen.tsx:                        onClick={(e) => {
src/components/SiteSelectionScreen.tsx:              onClick={(e) => e.stopPropagation()}
src/components/SiteSelectionScreen.tsx:                    onClick={() => {
src/components/SiteSelectionScreen.tsx:              onClick={(e) => e.stopPropagation()}
src/components/SiteSelectionScreen.tsx:                    onClick={() => {
src/components/SiteSelectionScreen.tsx:              onClick={(e) => e.stopPropagation()}
src/components/SiteSelectionScreen.tsx:                onClick={() => setShowReorgControlCenter(false)}
src/components/SiteSelectionScreen.tsx:                      onClick={handleExecuteReorg}
src/components/SuppliesSettingsTab.tsx:              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية إضافة مواد جديدة') : () => setIsMaterialModalOpen(true)}
src/components/SuppliesSettingsTab.tsx:                  onClick={() => setActiveMaterialCode(activeMaterialCode === item.code ? null : item.code)}
src/components/SuppliesSettingsTab.tsx:                    <div className="flex items-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity" onClick={e => e.stopPropagation()}>
src/components/SuppliesSettingsTab.tsx:                  onClick={() => setActiveMaterialCode(null)}
src/components/SuppliesSettingsTab.tsx:                onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية إضافة موردين') : () => setIsSupplierModalOpen(true)}
src/components/SuppliesSettingsTab.tsx:                    onClick={() => {
src/components/SuppliesSettingsTab.tsx:                      <div className="flex items-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity" onClick={e => e.stopPropagation()}>
src/components/SuppliesSettingsTab.tsx:                onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية إضافة طرق توريد') : () => setIsDeliveryMethodModalOpen(true)}
src/components/SuppliesSettingsTab.tsx:                      <div key={s.id} onClick={() => handleSelectSupplier(s)} className="px-5 py-3 hover:bg-indigo-50 cursor-pointer text-xs font-black text-slate-700 border-b border-slate-50 last:border-0 flex items-center justify-between group transition-colors">
src/components/SuppliesSettingsTab.tsx:                onClick={() => {
src/components/SuppliesSettingsTab.tsx:                onClick={() => {
src/components/SuppliesSettingsTab.tsx:          <div className="fixed inset-0 bg-black/50 z-[90] flex items-center justify-center p-4 animate-fadeIn" onClick={() => setSelectedDetailMethod(null)}>
src/components/SuppliesSettingsTab.tsx:            <div className="bg-white rounded-[2rem] p-8 w-full max-w-sm shadow-2xl overflow-hidden relative" onClick={e => e.stopPropagation()}>
src/components/DashboardOverview.tsx:              onClick={() => setActiveTab('weekly-report')}
src/components/FuelDashboard.tsx:            onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية إعادة تعيين البيانات') : handleResetData}
src/components/FuelDashboard.tsx:            onClick={() => window.print()}
src/components/FuelDashboard.tsx:            onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية استخدام المسح الذكي') : () => setShowAiIngest(!showAiIngest)}
src/components/FuelDashboard.tsx:            onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية تسجيل بونات جديدة') : () => {
src/components/FuelDashboard.tsx:                onClick={async () => {
src/components/FuelDashboard.tsx:                  onClick={() => {
src/components/FuelDashboard.tsx:                    onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية قيد السجلات من المسح الذكي') : () => {
src/components/FuelDashboard.tsx:                  onClick={() => toggleSort('date')}
src/components/FuelDashboard.tsx:                  onClick={() => toggleSort('quantity')}
src/components/FuelDashboard.tsx:                  onClick={() => toggleSort('cost')}
src/components/FuelDashboard.tsx:                        onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية تعديل السجلات') : () => startEditLog(log)}
src/components/FuelDashboard.tsx:                        onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية حذف السجلات') : () => handleDeleteLog(log.id, log.cost, log.equipmentName)}
src/components/FuelDashboard.tsx:                    onClick={() => setShowAddModal(false)}
src/components/FuelDashboard.tsx:                      onClick={() => setShowAddModal(false)}
src/components/FuelDashboard.tsx:                onClick={() => setConfirmState(null)} 
src/components/FuelDashboard.tsx:                onClick={() => {
src/components/FuelDashboard.tsx:                onClick={() => setConfirmState(null)}
src/components/BudgetAnalysis.tsx:              onClick={handleExportCSV}
src/components/BudgetAnalysis.tsx:              onClick={() => setConfirmOpen(true)}
src/components/BudgetAnalysis.tsx:                onClick={() => setConfirmOpen(false)} 
src/components/BudgetAnalysis.tsx:                onClick={() => {
src/components/BudgetAnalysis.tsx:                onClick={() => setConfirmOpen(false)}
src/components/BOQTab.tsx:                    onClick={ensureDefaultProject}
src/components/BOQTab.tsx:              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية إضافة بنود') : () => {
src/components/BOQTab.tsx:              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية استخدام الذكاء الاصطناعي') : () => {
src/components/BOQTab.tsx:                  onClick={() => {
src/components/BOQTab.tsx:                        onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية اعتماد البنود') : handleSaveExtractedItems}
src/components/BOQTab.tsx:                        onClick={() => setExtractedItems([])}
src/components/BOQTab.tsx:                                  onClick={() => setExtractedItems(extractedItems.filter(it => it.id !== item.id))}
src/components/BOQTab.tsx:              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية حفظ البنود') : handleAddItem}
src/components/BOQTab.tsx:                          onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => setEditingId(item.id)} 
src/components/BOQTab.tsx:                        onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDeleteItem(item.id)} 
src/components/ErpModulesForm.tsx:            onClick={() => setActiveFormSection('equipment')}
src/components/ErpModulesForm.tsx:            onClick={() => setActiveFormSection('qc')}
src/components/ErpModulesForm.tsx:            onClick={() => setActiveFormSection('hse')}
src/components/ErpModulesForm.tsx:            onClick={() => setActiveFormSection('wbs')}
src/components/ErpModulesForm.tsx:            onClick={() => setActiveFormSection('warehouse')}
src/components/LoginScreen.tsx:                    onClick={() => {
src/components/LoginScreen.tsx:                    onClick={() => {
src/components/LoginScreen.tsx:                      onClick={() => setShowPassword(!showPassword)}
src/components/LoginScreen.tsx:                      onClick={handleQuickDemoLoginDirect}
src/components/LoginScreen.tsx:                      onClick={handleQuickDemoLogin}
src/components/LoginScreen.tsx:                  onClick={handleGoogleLogin}
src/components/EquipmentModals.tsx:                      onClick={() => setShowAddEqModal(false)}
src/components/EquipmentModals.tsx:                      onClick={() => setShowAddEqModal(false)}
src/components/EquipmentModals.tsx:                      onClick={handleSaveEqEdit}
src/components/EquipmentModals.tsx:                      onClick={() => setEditingEqId(null)}
src/components/SuppliesCubicTab.tsx:          onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية إصدار محاضر جديدة') : handleOpenAdd}
src/components/SuppliesCubicTab.tsx:                      onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => handleOpenEdit(c)}
src/components/SuppliesCubicTab.tsx:                      onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDelete(c.id)}
src/components/SuppliesCubicTab.tsx:                onClick={handleToggleSelectAll}
src/components/SuppliesCubicTab.tsx:                      onClick={() => handleToggleTicket(ticket.id)}
src/components/SiteWorkersDashboard.tsx:        onClick={() => setIsOpen(!isOpen)}
src/components/SiteWorkersDashboard.tsx:            <div className="fixed inset-0 z-[60]" onClick={() => setIsOpen(false)} />
src/components/SiteWorkersDashboard.tsx:                  onClick={() => {
src/components/SiteWorkersDashboard.tsx:          onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية إضافة عاملين') : () => {
src/components/SiteWorkersDashboard.tsx:                           onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية تصفية القيود') : () => handleSettleWorker(worker.id, unsavedLogs)} 
src/components/SiteWorkersDashboard.tsx:                  onClick={() => { setSelectedWorker(w); setShowDetailsModal(true); setShowBasicInfo(false); }}
src/components/SiteWorkersDashboard.tsx:                          onClick={userRole === 'viewer' ? (e) => { e.stopPropagation(); alert('عذراً، لا تملك صلاحية الحذف'); } : (e) => { 
src/components/SiteWorkersDashboard.tsx:                          onClick={() => setShowBasicInfo(!showBasicInfo)}
src/components/SiteWorkersDashboard.tsx:                      onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => { 
src/components/SiteWorkersDashboard.tsx:                      onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => {
src/components/SiteWorkersDashboard.tsx:                      onClick={() => setDetailsSubTab('attendance')} 
src/components/SiteWorkersDashboard.tsx:                      onClick={() => setDetailsSubTab('payments')} 
src/components/SiteWorkersDashboard.tsx:                      onClick={() => setDetailsSubTab('history')} 
src/components/SiteWorkersDashboard.tsx:                                  onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية حذف الدفعات') : () => handleDeletePayment(p.id)} 
src/components/SiteWorkersDashboard.tsx:                  onClick={() => setShowWorkerModal(false)}
src/components/SiteWorkersDashboard.tsx:                        onClick={() => {
src/components/SiteWorkersDashboard.tsx:                        onClick={() => {
src/components/SiteWorkersDashboard.tsx:                    onClick={() => setShowWorkerModal(false)}
src/components/SiteWorkersDashboard.tsx:                onClick={() => handleDeleteWorker(confirmDelete.id, confirmDelete.name)}
src/components/SiteWorkersDashboard.tsx:                onClick={() => setConfirmDelete(null)}
src/components/SuppliesDashboard.tsx:          onClick={() => setActiveTab('accounts')}
src/components/SuppliesDashboard.tsx:          onClick={() => setActiveTab('records')}
src/components/SuppliesDashboard.tsx:          onClick={() => setActiveTab('cubic')}
src/components/SuppliesDashboard.tsx:          onClick={() => setActiveTab('settings')}
src/components/SubmissionsTab.tsx:            onClick={() => { setIsPrinting(false); setPrintSub(null); }}
src/components/SubmissionsTab.tsx:            onClick={() => window.print()}
src/components/SubmissionsTab.tsx:                  onClick={() => setStatusFilter(s)}
src/components/SubmissionsTab.tsx:            onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية إضافة طلبات فحص جديدة') : handleAddNewClick}
src/components/SubmissionsTab.tsx:                            onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية تعديل الطلبات') : () => handleEditClick(sub)}
src/components/SubmissionsTab.tsx:                            onClick={() => printDocument(sub)}
src/components/SubmissionsTab.tsx:                              onClick={() => handleDelete(sub.id)}
src/components/SubmissionsTab.tsx:                onClick={() => { setIsEditing(false); setSelectedSub(null); }}
src/components/SubmissionsTab.tsx:                    onClick={handleAddCustomWorkType}
src/components/SubmissionsTab.tsx:              onClick={() => { setIsEditing(false); setSelectedSub(null); }}
src/components/SubmissionsTab.tsx:              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية حفظ التعديلات') : undefined}
src/components/TransactionsTable.tsx:              onClick={() => window.print()}
src/components/TransactionsTable.tsx:                onClick={onAddClick}
src/components/TransactionsTable.tsx:                              onClick={saveEdit}
src/components/TransactionsTable.tsx:                              onClick={cancelEdit}
src/components/TransactionsTable.tsx:                            onClick={() => toggleRow(tx.id)}
src/components/TransactionsTable.tsx:                                onClick={() => startEdit(tx)}
src/components/TransactionsTable.tsx:                                onClick={() => {
src/components/TransactionsTable.tsx:                onClick={() => setConfirmState(null)} 
src/components/TransactionsTable.tsx:                onClick={() => {
src/components/TransactionsTable.tsx:                onClick={() => setConfirmState(null)}
src/components/SettingsTab.tsx:          onClick={() => setActiveSubTab('cloud')}
src/components/SettingsTab.tsx:          onClick={() => setActiveSubTab('local')}
src/components/SettingsTab.tsx:          onClick={() => setActiveSubTab('session')}
src/components/SettingsTab.tsx:                  onClick={currentUserRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية لرفع النسخ الاحتياطية') : handleDbBackupNow}
src/components/SettingsTab.tsx:                    onClick={currentUserRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية لربط الحسابات') : handleConnectGoogle}
src/components/SettingsTab.tsx:                    onClick={currentUserRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية لرفع النسخ الاحتياطية') : handleBackupNow}
src/components/SettingsTab.tsx:                onClick={fetchDbBackupsList}
src/components/SettingsTab.tsx:                        onClick={currentUserRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية للاستعادة') : () => handleRestoreDbBackup(file)}
src/components/SettingsTab.tsx:                        onClick={currentUserRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية للحذف') : () => handleDeleteDbBackup(file)}
src/components/SettingsTab.tsx:                  onClick={() => fetchBackupsList()}
src/components/SettingsTab.tsx:                          onClick={currentUserRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية للاستعادة') : () => handleRestoreCloud(file)}
src/components/SettingsTab.tsx:                          onClick={currentUserRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية للحذف') : () => handleDeleteCloudFile(file)}
src/components/SettingsTab.tsx:                  onClick={handleDownloadLocalFile}
src/components/GenericInputModal.tsx:              onClick={onClose}
src/components/EquipmentOperationLog.tsx:                            onClick={() => handleSaveLogIndex(realIdx)}
src/components/EquipmentOperationLog.tsx:                            onClick={() => {
src/components/EquipmentOperationLog.tsx:                          onClick={() => handleDeleteLogIndex(realIdx)}
src/components/EquipmentDashboard.tsx:                onClick={async () => { 
src/components/EquipmentDashboard.tsx:                onClick={() => setConfirmState(null)}
src/components/WeeklyExpenseReport.tsx:          onClick={() => setActiveSubTab('view')}
src/components/WeeklyExpenseReport.tsx:          onClick={() => setActiveSubTab('upload')}
src/components/WeeklyExpenseReport.tsx:                    onClick={() => setRawText(SAMPLE_REPORT_TEXT)}
src/components/WeeklyExpenseReport.tsx:                onClick={handleAnalyzeReport}
src/components/WeeklyExpenseReport.tsx:                    onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية استيراد الحسابات') : handleImportTransactions}
src/components/WeeklyExpenseReport.tsx:                    onClick={() => setAnalyzedTransactions([])}
src/components/WeeklyExpenseReport.tsx:                  onClick={() => handleJumpToPreset('demo')}
src/components/WeeklyExpenseReport.tsx:                  onClick={() => handleJumpToPreset('today')}
src/components/WeeklyExpenseReport.tsx:                  onClick={() => handleJumpToPreset('prev')}
src/components/WeeklyExpenseReport.tsx:                      onClick={handlePrevWeek} 
src/components/WeeklyExpenseReport.tsx:                      onClick={handleNextWeek} 
src/components/WeeklyExpenseReport.tsx:                  onClick={handlePrint}
src/components/WeeklyExpenseReport.tsx:                  onClick={() => {
src/components/WeeklyExpenseReport.tsx:                    onClick={handleResetTree}
src/components/WeeklyExpenseReport.tsx:                            onClick={() => setEditSelectedCategory(catName)}
src/components/WeeklyExpenseReport.tsx:                                onClick={(e) => {
src/components/WeeklyExpenseReport.tsx:                                onClick={(e) => {
src/components/WeeklyExpenseReport.tsx:                                onClick={() => handleEditItemName(editSelectedCategory, it)}
src/components/WeeklyExpenseReport.tsx:                                onClick={() => handleRemoveItem(editSelectedCategory, it)}
src/components/WeeklyExpenseReport.tsx:                  onClick={userRole === 'viewer' ? (e) => { e.preventDefault(); alert('عذراً، لا تملك صلاحية التسجيل'); } : undefined}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => handleStartEditRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDeleteRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => handleStartEditRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDeleteRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => handleStartEditRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDeleteRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => handleStartEditRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDeleteRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => handleStartEditRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDeleteRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية التعديل') : () => handleStartEditRow(item)}
src/components/WeeklyExpenseReport.tsx:                              onClick={userRole === 'viewer' ? () => alert('عذراً، لا تملك صلاحية الحذف') : () => handleDeleteRow(item)}
src/components/WeeklyExpenseReport.tsx:                onClick={() => setEditingItem(null)} 
src/components/WeeklyExpenseReport.tsx:                onClick={handleSaveChanges}
src/components/WeeklyExpenseReport.tsx:                onClick={() => setEditingItem(null)}
src/components/WeeklyExpenseReport.tsx:                onClick={() => setConfirmState(null)} 
src/components/WeeklyExpenseReport.tsx:                onClick={() => {
src/components/WeeklyExpenseReport.tsx:                onClick={() => setConfirmState(null)}
src/components/NotificationsTab.tsx:          onClick={() => setActiveTab('received')}
src/components/NotificationsTab.tsx:            onClick={() => setActiveTab('send')}
src/components/NotificationsTab.tsx:                                onClick={() => handleDismissNotification(notif.id, notif.dismissedBy || [])}
src/components/NotificationsTab.tsx:                                onClick={() => handleDeleteNotification(notif.id)}
